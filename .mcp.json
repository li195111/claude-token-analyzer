{
  "mcpServers": {
    "token-analyzer": {
      "command": "${CLAUDE_PLUGIN_DATA}/cta-mcp-server",
      "args": [],
      "env": {
        "CTA_DB_PATH": "${CLAUDE_PLUGIN_DATA}/token-analyzer.db"
      }
    }
  }
}
